#!/bin/bash
# TP=2 MiniMax-M2.5 INT4 AutoRound: vLLM Serve — 65K Context
# Vanilla (kein Speculative Decoding)

set -euo pipefail

CONTAINER_NAME="minimax-head"
MODEL_PATH="/data/tensordata/MiniMax-M2.5-int4-AutoRound/MiniMax-M2.5-w4g128"
PORT=8011
LOGFILE="/tmp/minimax-tp2-serve-65k.log"

echo "=== Ray Cluster Status pruefen ==="
podman exec "$CONTAINER_NAME" ray status
echo ""

echo "=== vLLM serve starten (TP=2, 65K Context) ==="
echo "Modell:  $MODEL_PATH"
echo "Port:    $PORT"
echo "Context: 65536"
echo ""

nohup podman exec "$CONTAINER_NAME" \
  vllm serve "$MODEL_PATH" \
    --host 0.0.0.0 \
    --port "$PORT" \
    --served-model-name minimax-m2.5 \
    --tensor-parallel-size 2 \
    --distributed-executor-backend ray \
    --gpu-memory-utilization 0.05 \
    --kv-cache-memory-bytes 30G \
    --max-model-len 65536 \
    --trust-remote-code \
    --quantization auto_round \
    --reasoning-parser minimax_m2 \
    --disable-log-requests \
  > "$LOGFILE" 2>&1 &

SERVE_PID=$!
echo "vLLM serve gestartet (PID: $SERVE_PID)"
echo "Logs: tail -f $LOGFILE"
echo "Health-Check: curl http://localhost:$PORT/health"
