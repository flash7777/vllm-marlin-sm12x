#!/bin/bash
# TP=2 Qwen3-Coder-Next INT4 AutoRound: HEAD Container (DGX Spark)
# torchrun-based (kein Ray)

set -euo pipefail

CONTAINER_NAME="qwennext-tp2-head"
IMAGE="localhost/vllm-unified"
CX7_IF="enp1s0f0np0"

echo "=== torchrun HEAD Container starten (DGX Spark) ==="

podman stop "$CONTAINER_NAME" 2>/dev/null || true
podman rm "$CONTAINER_NAME" 2>/dev/null || true

podman run -d \
  --name "$CONTAINER_NAME" \
  --network host \
  --ipc=host \
  --device nvidia.com/gpu=all \
  --device /dev/infiniband/uverbs0 \
  --device /dev/infiniband/rdma_cm \
  --security-opt=label=disable \
  --hooks-dir=/usr/share/containers/oci/hooks.d \
  -v /data/tensordata:/data/tensordata \
  -v "$HOME/.cache/huggingface:/root/.cache/huggingface" \
  -e NCCL_SOCKET_IFNAME="$CX7_IF" \
  -e GLOO_SOCKET_IFNAME="$CX7_IF" \
  -e TP_SOCKET_IFNAME="$CX7_IF" \
  -e NCCL_IB_DISABLE=0 \
  -e NCCL_IB_HCA=rocep1s0f0 \
  -e NCCL_DEBUG=WARN \
  -e VLLM_MLA_DISABLE=1 \
  -e FLASHINFER_DISABLE_VERSION_CHECK=1 \
  -e VLLM_ENABLE_V1_MULTIPROCESSING=0 \
  "$IMAGE" \
  sleep infinity

echo "Container '$CONTAINER_NAME' gestartet."
echo "Naechster Schritt: ./start.qwennext_tp2.worker"
